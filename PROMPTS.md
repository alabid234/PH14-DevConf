I didn't know which Agent Im using. Its says local here.

What is your name? => My name is GitHub Copilot. 

The codes starts from here:
1. "/init"
2. "
    You are a Senior Frontend Developer. Generate a modern, responsive Sponsors section.
    To generate it:
    - Add the HTML inside the `<section id="Sponsors">` in `index.html`.
    - Add the CSS in `main.css` under `/* Write css for Sponsors Section Style here */`.
    - Use all sponsor logos from `assets/Sponsors`.
    - Match the existing website theme with a clean, professional UI/UX.
    - You are only allowed to use HTML and CSS.
"
3. "
    Most of the logo contain white color. You should centralize the images and make make the background darker. U must consider the page theme.
"
4. "Change the border colors to --clr-primary-d of the sponsor-items."
